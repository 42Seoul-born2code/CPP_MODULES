name: C/C++ CI

on:
  [ "push" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: make
      run: |
        cd ex00 
        make
        ls
        cd ../ex01 
        make
        ls